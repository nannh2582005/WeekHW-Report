# WeekHW-Report

## Build instructions

This report uses `fontspec`, so it must be compiled with XeLaTeX.

Recommended commands:

- `xelatex -interaction=nonstopmode main.tex`
- `latexmk -pdf -xelatex main.tex`

